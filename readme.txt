run the install.cmd(to install gulp and all depndancies)
run the console.cmd(to open console and run gulp command)


Development work Enviroment html_source
Commands:-
	gulp
	to escape the watch:-  ctrl+c


For publishing project to html_public
Commands:-
	gulp clean
	gulp build
	gulp minify